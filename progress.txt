# progress.txt — Estado de avance del Simulador VR Oftalmologico

Para el plan completo de sprints (todos los hitos, criterios de salida y estimaciones)
ver `PLAN.md` en la raiz. Este archivo es solo el estado actual y notas de la sesion.

================================================================
SPRINT 0 — Setup del proyecto
================================================================
Estado: CERRADO ✅

Validacion: APK exportado con Gradle Build + Meta Plugin instalado y
funcionando en Quest 2. La app arranca en modo VR sin errores de OpenXR.

Fixes clave descubiertos durante el sprint:
  - Plugin `godotopenxrvendors` instalado desde AssetLib.
  - Export preset: xr_mode=OpenXR, enable_meta_plugin=true.
  - Export preset: gradle_build/use_gradle_build=true (CRITICO — sin esto
    el binario del Meta OpenXR loader no se incluye en el APK).
  - Quest 2/3/Pro support marcado en Meta XR Features.

================================================================
SPRINT 1 — F1 minima (OpenXR + VIEW_INDEX)
================================================================
Estado: CERRADO ✅

Validacion: en Quest 2, ojo izquierdo ve rojo y ojo derecho ve azul.
Confirma que toda la cadena VIEW_INDEX -> multiview -> renderer estereo
funciona, que es la base del Blend asimetrico de F2.

Entregables:
  - features/vr_core/main.tscn (XROrigin3D + XRCamera3D + EyeTestQuad +
    LeftHand/RightHand controllers).
  - features/vr_core/main.gd (bootstrap OpenXR + use_xr + physics 90Hz).
  - features/vision_shaders/eye_test.gdshader (shader spatial unshaded
    con `if VIEW_INDEX == VIEW_MONO_LEFT` rojo / else azul).
  - project.godot run/main_scene apuntando a la escena.

Notas operativas:
  - El test no requiere controllers: se valida solo con la vision.
  - Quest 2 con hand tracking activo en Settings -> Movimiento permite
    operar el sistema sin pilas en los controles.

================================================================
SPRINT 2 — F2 PoC GO/NO-GO ⚠️ BLOQUEANTE
================================================================
Estado: EN CURSO

Objetivo: medir si el simulador es viable a >=72 FPS en Quest 2 con
post-procesado asimetrico por ojo.

Detalle completo y criterios de salida en PLAN.md.
Protocolo de medicion y tabla de resultados en MEASUREMENTS.md.

Hecho:
  [x] features/vision_shaders/sprint2_blur_test.gdshader: shader spatial
      con hint_screen_texture + hint_depth_texture, box blur 9-tap
      asimetrico via VIEW_INDEX, halo via threshold + LOD alto, conversion
      depth-a-metros con INV_PROJECTION_MATRIX.
  [x] features/vr_core/main.tscn: escena Sprint 2 con WorldEnvironment +
      DirectionalLight3D + TestEnvironment (floor, 3 cubos a 0.4m/2m/10m
      con labels, esfera emissive para halo) + XROrigin3D con XRCamera3D
      conteniendo PostProcessQuad y FpsHud.
  [x] features/vr_core/main.gd: aplica preset asimetrico al arrancar
      (izq=PanOptix simulado, der=Vivity simulado), configura FSR
      scaling_3d_scale=0.85, calcula FPS y frame time en _process().
  [x] MEASUREMENTS.md: protocolo de medicion completo con criterio GO/NO-GO,
      tabla para Runs, variantes a probar si NO-GO.

Pendiente NO automatizable (te toca a vos):
  [ ] Re-abrir el editor de Godot (importa el shader y la escena nuevos).
  [ ] Re-exportar:
        godot --headless --path . --export-debug "Android" Simulador.apk
  [ ] Re-instalar:
        adb install -r Simulador.apk
  [ ] (Recomendado) Instalar OVR Metrics Tool en el Quest:
        https://developer.oculus.com/downloads/package/ovr-metrics-tool/
        adb install -r OVRMetricsTool.apk
  [ ] Lanzar el simulador. Observar:
        - Una escena 3D con piso gris, 3 cubos (rojo cerca, verde medio, azul lejos)
          y una esfera blanca brillante.
        - El FpsHud arriba a la derecha mostrando FPS y frame time.
        - Cada ojo ve un nivel de blur DIFERENTE en cada cubo:
            * Ojo izq: blur_near=0.5 medium=0.2 far=0.1, halo=0.7
            * Ojo der: blur_near=0.8 medium=0.1 far=0.0, halo=0.2
          Tapando un ojo y luego el otro, la diferencia debe ser obvia.
        - La esfera brillante muestra halos solo en el ojo izquierdo (intensidad 0.7).
  [ ] Seguir protocolo de MEASUREMENTS.md y completar Run 1.
  [ ] Reportar resultados aca (FPS, frame time, veredicto).
