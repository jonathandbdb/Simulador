# progress.txt â€” Estado de avance del Simulador VR Oftalmologico

Para el plan completo de sprints (todos los hitos, criterios de salida y estimaciones)
ver `PLAN.md` en la raiz. Este archivo es solo el estado actual y notas de la sesion.

================================================================
SPRINT 0 â€” Setup del proyecto
================================================================
Estado: CERRADO âœ…

Validacion: APK exportado con Gradle Build + Meta Plugin instalado y
funcionando en Quest 2. La app arranca en modo VR sin errores de OpenXR.

Fixes clave descubiertos durante el sprint:
  - Plugin `godotopenxrvendors` instalado desde AssetLib.
  - Export preset: xr_mode=OpenXR, enable_meta_plugin=true.
  - Export preset: gradle_build/use_gradle_build=true (CRITICO â€” sin esto
    el binario del Meta OpenXR loader no se incluye en el APK).
  - Quest 2/3/Pro support marcado en Meta XR Features.

================================================================
SPRINT 1 â€” F1 minima (OpenXR + VIEW_INDEX)
================================================================
Estado: CERRADO âœ…

Validacion: en Quest 2, ojo izquierdo ve rojo y ojo derecho ve azul.
Confirma que toda la cadena VIEW_INDEX -> multiview -> renderer estereo
funciona, que es la base del Blend asimetrico de F2.

================================================================
SPRINT 2 â€” F2 PoC GO/NO-GO âš ï¸ BLOQUEANTE
================================================================
Estado: CERRADO âœ… (GO en Quest 2)

Run 1 Quest 2: FPS min/avg/max 69/70/72, frame time ~13 ms, sin stuttering.
Criterio GO cumplido (>=72 FPS estable, frame time <budget 72Hz = 13.88 ms).

Iteracion 1 sobre shader (cerrada):
  - BLUR_RADIUS_PX: 4 -> 12 (blur diferenciable).
  - Halo: 2 anillos concentricos + falloff cuadratico + tinte amarillento
    (mas realista que el glow plano original; aproxima el efecto difractivo
    de IOLs multifocales).
  - Sin cambio de samples GPU.

Fixes de compatibilidad (al integrar a Quest Link descubrimos que multiview
+ mipmaps + FSR rompen el framebuffer en Godot 4.6):
  - filter_linear (sin mipmaps) en hint_screen_texture.
  - FSR scaling_3d desactivado (comentado en main.gd; reactivar solo en
    Quest standalone si se necesita).

Pendiente para iteraciones futuras (no bloqueante, Sprint 5 lo retoma):
  - Halo cromatico real (sampleo R/G/B en radios distintos): +6 samples.
  - Tercer anillo difractivo para PanOptix puro.
  - Flares radiales para monofocales asfericos.
  - Reactivar FSR cuando el deployment sea solo sideload.

================================================================
SPRINT 3 â€” F0 backend minimo
================================================================
Estado: EN CURSO (fase local CERRADA âœ…, fase VPS pendiente)

Objetivo: API REST publica desplegada en el VPS (backend FastAPI + Postgres
+ MinIO + Caddy con TLS automatico).

Hecho:
  [x] backend/docker-compose.yml: 4 servicios (api, db, bucket, caddy).
      Volumen ../defaults/lentes.json montado en /seed/lentes.json del api.
  [x] backend/Caddyfile: reverse proxy, /healthz directo, scheme via
      env var ($SCHEME) â€” SCHEME=http:// en local, vacio en prod.
  [x] backend/.env.example: todas las variables documentadas.
  [x] backend/api/Dockerfile: Python 3.12-slim + libpq5 + curl healthcheck.
  [x] backend/api/requirements.txt: FastAPI 0.115.5, SQLModel 0.0.22,
      psycopg[binary] 3.2.3, slowapi 0.1.9, passlib[bcrypt] 1.7.4,
      bcrypt 4.0.1 (pineado, fix incompatibilidad con bcrypt 4.1+),
      boto3 1.35.84.
  [x] backend/api/app/{config,database,models,seed,routers,main}.py
      â€” todos los modulos del Sprint 3.
  [x] backend/README.md actualizado con stack + quick start local + prod.

Validacion local (todos los smoke tests pasaron):
  [x] Docker Desktop instalado via winget en Windows + WSL2.
  [x] docker compose up -d --build levanto los 4 servicios.
  [x] Seed inicial corrio: admin user + 3 lentes (desde defaults/lentes.json)
      + version dummy v0.1.0 + DEV_TEST_001 device.
  [x] GET /healthz                          â†’ 200 "ok"
  [x] GET /api/manifest.json                 â†’ JSON valido con version dummy
  [x] GET /api/lenses                        â†’ JSON con 3 lentes seed
  [x] POST /api/verify (DEV_TEST_001)        â†’ 200 status=ok
  [x] POST /api/verify (DESCONOCIDO)         â†’ 403 DEVICE_NOT_FOUND
  [x] POST /api/verify (2do en <1 min)       â†’ 429 rate limit (feature OK)
  [x] http://localhost:8080/docs Swagger UI accesible.

Bugs encontrados y resueltos:
  - docker-credential-desktop no en PATH de bash â†’ workaround con
    export PATH="/c/Program Files/Docker/Docker/resources/bin:$PATH"
    (no es fix permanente, solo en sesiones bash de Claude).
  - passlib 1.7.4 + bcrypt 4.1+ incompatibles â†’ pin bcrypt==4.0.1.
  - Caddy default sirve TLS en localhost â†’ forzar SCHEME=http:// via
    env var, prefijo http:// en site address del Caddyfile.

Pendiente Sprint 3 fase 2 (deploy a VPS):
  [ ] Conseguir VPS (Hetzner CX22 ~3 EUR/mes recomendado).
  [ ] Apuntar dominio o subdominio al VPS via DNS A record.
  [ ] SSH al VPS, instalar docker + compose plugin.
  [ ] Clonar el repo en el VPS.
  [ ] Editar .env del VPS:
        DOMAIN=api.tu-dominio.com
        SCHEME=                 (vacio = Caddy auto HTTPS)
        PORT=443
        PUBLIC_BASE_URL=https://api.tu-dominio.com
        POSTGRES_PASSWORD=<openssl rand -hex 24>
        JWT_SECRET=<openssl rand -hex 32>
        ADMIN_DEFAULT_PASS=<password fuerte real>
        API_KEY_CI=<openssl rand -hex 32>
  [ ] docker compose up -d. Caddy emite cert automaticamente.
  [ ] Smoke test desde otra red:
        curl https://api.tu-dominio.com/healthz

Criterio de salida del Sprint 3:
  curl https://api.tu-dominio.com/api/manifest.json retorna 200
  con JSON valido desde el VPS de produccion.

================================================================
SPRINT 4 â€” F1 completa
================================================================
Estado: EN CURSO

Objetivo: simulador con catalogo dinamico desde backend + entorno 3D con
transicion dia/noche con fade negro.

Hecho:
  [x] autoloads/data_manager.gd: rewrite completo.
      - Carga prioritaria: user://lentes.json (cache) -> res://defaults/
        lentes.json (embebido) -> sync background con backend.
      - HTTPRequest interno con timeout 5 s + use_threads=true.
      - Si backend responde 200 con JSON valido, sobrescribe cache y emite
        catalog_synced_with_backend(version).
      - Si falla (offline, 5xx, timeout, JSON invalido), emite
        catalog_sync_failed(reason) y se queda con el catalogo local.
      - get_lens(id), get_lens_ids(), apply_lens(id, eye), refresh_from_backend().
      - Configuracion: const BACKEND_URL := "http://localhost:8080".
        Cambiar al exportar para Quest: http://<IP_DE_LA_PC>:8080.
  [x] features/vision_shaders/fade.gdshader: shader spatial unshaded
      blend_mix con uniform alpha hint_range(0,1). Para fade-to-black.
  [x] features/vr_core/main.tscn: agregados FadeQuad (priority 12, hijo
      de XRCamera3D, 4x4 a z=-0.05) y SyncHud (Label3D al lado izq, verde,
      muestra estado del DataManager).
  [x] features/vr_core/main.gd: reescrito para Sprint 4.
      - Conecta a las 3 senales del DataManager y muestra estado en SyncHud.
      - Auto-cicla dia/noche cada 15 s (DAY_NIGHT_INTERVAL).
      - Tween secuencial: fade-out 0.8s -> swap environment -> hold 0.2s
        -> fade-in 0.8s. Total ~2 s por transicion.
      - _apply_environment(params) cambia DirectionalLight.light_energy,
        Environment.ambient_*, ProceduralSkyMaterial.sky/ground colors.
      - FpsHud incluye ahora "modo: dia/noche" para debug.
      - Si OpenXR no esta inicializado, push_warning en lugar de push_error
        â€” permite probar la escena en el editor de PC sin Quest.

Pendiente NO automatizable (te toca a vos):

  Test 1 â€” En editor de Godot (PC, sin VR):
  [ ] Abrir el proyecto en Godot.
  [ ] Asegurarse de que el backend local sigue corriendo
      (docker compose ps en backend/, deben verse los 4 contenedores).
  [ ] Hit Play en el editor. La escena debe abrirse en modo no-VR
      (push_warning sobre OpenXR es esperable; el resto funciona).
  [ ] Validar:
        - FpsHud arriba a la derecha: FPS + modo: dia (al inicio).
        - SyncHud arriba a la izquierda: "loaded(defaults) 3 lentes" o
          "loaded(backend) 3 lentes" / "backend OK [...]" si el sync paso.
        - A los ~15 s, fade negro de ~2 s, vuelve la imagen en modo "noche"
          (oscuro, luz tenue, esfera brillante destacada).
        - A los ~30 s vuelve a dia. Asi indefinidamente.

  Test 2 â€” En Quest 2 standalone:
  [ ] Averiguar IP de la PC en la LAN:
        ipconfig | findstr IPv4
      (Ejemplo: 192.168.1.42)
  [ ] Editar autoloads/data_manager.gd:
        const BACKEND_URL := "http://192.168.1.42:8080"
  [ ] Permitir el puerto 8080 en el firewall de Windows:
        - PowerShell admin:
          New-NetFirewallRule -DisplayName "Simulador backend 8080" `
            -Direction Inbound -Protocol TCP -LocalPort 8080 -Action Allow
        - O via UI: Panel de Control -> Firewall de Windows -> Reglas de
          entrada -> Nueva regla -> Puerto -> TCP 8080 -> Permitir.
  [ ] Confirmar que el Quest esta en el mismo wifi que la PC.
  [ ] Desde el Quest, abrir un navegador (o usar Chrome remoto):
        http://192.168.1.42:8080/healthz
      Debe responder "ok". Si no, el firewall sigue bloqueando o las
      redes wifi son distintas.
  [ ] Re-exportar e instalar el APK:
        godot --headless --path . --export-debug "Android" Simulador.apk
        adb install -r Simulador.apk
  [ ] Lanzar el simulador. Validar:
        - SyncHud muestra "loaded(defaults)" inicialmente.
        - En 1-3 s, cambia a "backend OK [0.0.1-seed]" (sync exitoso).
        - Ciclo dia/noche corre.

Criterio de salida del Sprint 4:
  1. El simulador arranca y carga el catalogo desde una fuente cualquiera
     (cache, defaults o backend).
  2. Si el backend esta accesible, sync con el backend tuvo exito al menos
     una vez (SyncHud muestra "backend OK").
  3. La transicion dia/noche funciona con fade negro intermedio sin pisar
     el render del shader de Sprint 2.
  4. El FPS se mantiene en el mismo orden que Sprint 2 (>=72 estable en
     Quest 2) â€” el fade no debe penalizar el frame time.

Bug encontrado y resuelto durante el Test 2 (acceso desde LAN):
  - Sintoma: desde Quest, celular y otra PC en la misma wifi, el backend
    devolvia pagina en blanco / "inalcanzable". Desde localhost en la PC
    host funcionaba perfecto. Firewall ya estaba abierto y Docker bindeaba
    0.0.0.0:8080 (verificado con netstat).
  - Causa: en backend/.env teniamos DOMAIN=localhost. El Caddyfile usa
    `{$SCHEME}{$DOMAIN:localhost}:{$PORT:8080}` como direccion del sitio,
    asi que Caddy solo aceptaba requests cuyo header Host fuera
    "localhost:8080". Cualquier request de la LAN llega con Host=<IP>:8080
    y Caddy no matcheaba ningun sitio -> respuesta vacia.
  - Fix: dejar DOMAIN= vacio en .env (modo local). Caddy interpreta
    http://:8080 como "escuchar en todos los hostnames". En prod volver
    a setear DOMAIN=api.tu-dominio.com (necesario para que Caddy emita
    el cert TLS especifico para ese host).
  - Comando para aplicar: docker compose up -d --force-recreate caddy
    (solo `up -d` no recrea si no cambio el compose; force-recreate si).

Bug encadenado descubierto enseguida (Test 2, segunda pasada):
  - Sintoma: tras cambiar DOMAIN= en .env, Caddy seguia devolviendo
    Content-Length: 0 a peticiones desde la IP de LAN.
  - Diagnostico via `docker exec backend-caddy-1 caddy adapt`: la config
    JSON adaptada mostraba "match":[{"host":["localhost"]}], es decir,
    Caddy seguia matcheando solo Host=localhost.
  - Causa real: en docker-compose.yml la linea era
        DOMAIN: ${DOMAIN:-localhost}
    La sintaxis `${VAR:-default}` (con dos puntos) cae al default si la
    var esta UNSET *o* VACIA. Por eso DOMAIN= se ignoraba y volvia a
    localhost antes de llegar al container.
  - Fix: cambiar a `${DOMAIN-localhost}` (sin dos puntos): solo cae al
    default si la var esta unset; acepta string vacio como valor valido.
  - Bug paralelo: BACKEND_URL en autoloads/data_manager.gd estaba en
    "http://localhost:8080". En Quest standalone, localhost = el Quest,
    no la PC. RESULT_CANT_CONNECT (2) en HTTPRequest.
    Fix temporal: hardcodear "http://192.168.2.12:8080".
    Pendiente: hacer BACKEND_URL configurable en runtime (user://
    backend_url.txt o ProjectSettings.get_setting con override) para no
    reexportar el APK cada vez que cambia la IP de LAN.

Estado al cierre de esta sesion del Sprint 4:
  - Backend accesible desde LAN: validado con curl desde otra red ->
    HTTP 200 + JSON completo (1422 bytes).
  - APK reexportado con Godot 4.6.1 (template instalado) e instalado en
    Quest via adb install -r. Faltaba ejecutar Test 1 (editor PC) y
    confirmar SyncHud=backend OK en Quest tras el fix de Caddy.
Intento de mejora descartado (BACKEND_URL configurable en runtime):
  - Se probo un resolver runtime con prioridad
    (1) user://backend_url.txt -> (2) ProjectSetting -> (3) const.
  - No funciono en el Quest (siguio dando inalcanzable a pesar de pushear
    backend_url.txt a /sdcard/Android/data/com.simulador.vr/files/).
  - REVERTIDO: data_manager.gd vuelve a tener
    `const BACKEND_URL := "http://192.168.2.12:8080"` hardcodeado.
    project.godot vuelve a no tener application/config/backend_url.
  - TODO post-Sprint 5: investigar por que el resolver no leyo el archivo
    en Quest (posible permiso de scoped storage en Android 11+).

Sprint 4 CERRADO:
  - Catalogo carga desde defaults y sync con backend OK confirmado en
    Quest 2 (SyncHud muestra "backend OK [0.0.1-seed]").
  - Transicion dia/noche con fade negro funciona, no pisa el shader F2.
  - FPS estables en el mismo orden que Sprint 2 (>=72 en Quest 2).
  - APK actual: hardcode `http://192.168.2.12:8080` (LAN de dev).


==============================================================
Sprint 5 - F2 completa (lentes del catalogo aplicadas en runtime)
==============================================================

Cambios en features/vr_core/main.gd:
  - Eliminado el PRESET hardcodeado. Ahora la fuente de verdad para los
    uniforms del shader es DataManager.current_vision_state, alimentado
    via DataManager.apply_lens(lens_id, eye).
  - Suscripcion a DataManager.vision_state_changed(eye, params): cada
    cambio actualiza los uniforms del shader unificado halo+DoF
    (sprint2_blur_test.gdshader) usando SHADER_PARAM_MAP.
  - Al cargar el catalogo (sea de cache, defaults o backend) se aplica
    la lente INITIAL_LENS_ID="monofocal" a ambos ojos. Fallback a la
    primera lente del catalogo si no existe.
  - Input handler del XRController3D derecho:
      * boton A  (ax_button)    -> proxima lente para ojo izquierdo
      * boton B  (by_button)    -> proxima lente para ojo derecho
      * trigger  (trigger_click) -> reset Blend (misma lente en ambos)
    Asi se habilita el modo Blend (lentes distintas por ojo) sin UI
    explicita: el "modo Blend" emerge cuando A y B dejan los indices
    desfasados.
  - SyncHud se reutiliza para mostrar la lente activa por ojo:
        "L: monofocal
         R: panoptix (Blend)"

Criterio de salida del Sprint 5:
  1. Cambiar de lente con el control derecho produce una diferencia
     visual notable (blur o halo) en al menos 3 lentes del catalogo.
  2. Modo Blend funciona: ojo izquierdo y ojo derecho pueden mostrar
     simultaneamente parametros de lentes distintas.
  3. FPS estables en el mismo orden que Sprint 2 (>=72 en Quest 2).
  4. No hay PRESET hardcodeado en main.gd.

Pendiente de validacion en Quest (siguiente sesion):
  - Confirmar que el SyncHud muestra "L: monofocal / R: monofocal" al
    arrancar y va cambiando al apretar A/B.
  - Confirmar diferencias visuales notables entre monofocal/panoptix/vivity.
  - Medir FPS con el modo Blend mas asimetrico posible (panoptix vs vivity).

=== Sprint 6 (F3 PoC streaming) ï¿½ Implementacion (pendiente medicion) ===
Fecha: 2026-05-26 16:41
Archivos nuevos:
  - autoloads/streaming_server.gd (WebSocket server en puerto 9090)
  - features/tablet/streaming_capture.gd (SubViewport 512x512 @ 15 Hz, JPG q=0.5)
  - features/tablet/streaming_client.tscn + .gd (cliente PC Godot)
Cambios:
  - project.godot: autoload StreamingServer registrado.
  - features/vr_core/main.tscn: Label3D StreamHud agregado (FpsHud, SyncHud, StreamHud).
  - features/vr_core/main.gd: _setup_streaming_capture + _update_stream_hud cada 0.5s.
IP Quest detectada por adb: 192.168.2.30
Proximo paso: arrancar APK, correr streaming_client.tscn en PC, medir FPS visor.
Criterio GO: >=72 FPS con streaming activo.


=== Sprint 6 CERRADO (GO) ï¿½ 2026-05-26 17:03 ===
Primer test: 512x512 @ 15 Hz + JPG q=0.5 en main thread => FPS cayo a 30-40 (NO-GO).
Optimizaciones aplicadas:
  - SubViewport 512 -> 256 (4x menos pixels).
  - Tick 15 Hz -> 10 Hz.
  - JPG quality 0.5 -> 0.4.
  - save_jpg_to_buffer movido a WorkerThreadPool (call_deferred al main thread para broadcast).
Segundo test: estable en Quest 2 (criterio GO cumplido). En Quest 3 deberia ser holgado.
Riesgo R2 (captura sincrona stutters VR) -> MITIGADO.
Nota: handshake con device_id (entregable Sprint 7) se pospone a Sprint 9 porque depende de licencias.


=== Sprint 7 CERRADO (F3 completa) - 2026-05-26 ===
Objetivo: tablet de control funcional con UI dinamica desde catalogo.

Cambios principales:
  - autoloads/streaming_server.gd:
      * Senal command_received(cmd: Dictionary, peer_id) para comandos JSON
        de la tablet (apply_lens, etc.).
      * Texto bidireccional: send_text_to(peer_id, text), broadcast_text(text).
      * Bug fix: client_connected ahora se emite solo en la transicion
        STATE_CONNECTING -> STATE_OPEN (antes se emitia al accept TCP, lo que
        causaba que el "hello" del visor se descartara silenciosamente porque
        el WebSocketPeer aun no habia completado el handshake HTTP-Upgrade).
        Se cachea host del stream en _peer_hosts y un flag _peer_handshaked.
      * Guard: si feature "tablet" esta activa NO se levanta el server.
  - features/vr_core/main.gd:
      * _on_streaming_client_connected: envia mensaje hello con catalog_version,
        lista de lentes (clave "catalogo" del JSON) y vision_state actual.
      * _on_streaming_command_received: valida lens_id contra DataManager.get_lens_ids,
        sincroniza _lens_index_left/right y llama a DataManager.apply_lens.
  - features/tablet/streaming_client.tscn + .gd: UI completa rediseÃ±ada.
      * Layout: panel izquierdo con stream (HeaderLabel + TopBar + DiscoveredList
        + StatusLabel + StreamFrame con TextureRect) y panel derecho con control
        (Title + EyeOption Ambos/Izquierdo/Derecho + LensListScroll + StateLabel).
      * Tema con fondo oscuro, paneles redondeados, jerarquia tipografica.
      * _rebuild_lens_list: crea botones por cada lente recibida del hello.
      * Estado optimista en cliente: al tocar un boton actualiza StateLabel
        ("L: monofocal / R: panoptix (Blend)") sin esperar respuesta.
  - autoloads/discovery_beacon.gd (NUEVO): autoload UDP broadcast LAN discovery.
      * Visor: cada 2s emite broadcast a 255.255.255.255:9091 con
        {app, device_id, ws_port, ts}. Funciona sin backend, sin internet.
      * Tablet (detecta feature "tablet"): bindea 0.0.0.0:9091, cachea hosts
        vistos, emite visor_discovered(host, payload). La UI lista los hosts
        activos como botones; un toque conecta.
  - export_presets.cfg: nuevo preset "AndroidTablet" (package com.simulador.tablet,
    xr_mode=0, enable_meta_plugin=false, custom_features="tablet").
  - project.godot: run/main_scene.tablet="res://features/tablet/streaming_client.tscn"
    + autoload DiscoveryBeacon.

Optimizaciones de calidad (iteracion final del sprint):
  - StreamingCapture: 256x256 -> 320x320, JPG q=0.4 -> 0.55 (notablemente
    mas nitido sin caida de FPS perceptible en Quest 2).
  - HUDs del visor (FpsHud, SyncHud, StreamHud): pixel_size 0.005 -> 0.0015
    para que no estorben la visual. Reposicionados mas al centro.

Criterio de salida cumplido:
  - Desde la tablet se cambia de lente y el visor responde.
  - Modo Blend funciona desde la tablet (eje "Izquierdo"/"Derecho" en el
    selector + lentes distintas).
  - Discovery automatico: el visor aparece en la tablet sin necesidad de
    teclear IP (input manual sigue como fallback).

Mejoras pendientes para Sprint 8+ (no bloqueantes):
  - WifiManager.MulticastLock en Android: en algunos casos Android filtra
    broadcasts UDP cuando el wifi entra en standby. Si en pruebas en campo
    el discovery falla, agregar permiso CHANGE_WIFI_MULTICAST_STATE + plugin.
  - Subir mas la resolucion del stream cuando podamos medir margen real
    en Quest 3 (objetivo: 480x480).
  - Indicador visual en la tablet de "conexion estable" vs "reconectando".
  - Eliminar los HUDs Label3D del visor cuando exista UI de control real
    (post-Sprint 8 o Sprint 12).

Pendientes formales heredados (no del Sprint 7):
  - Sprint 3 fase 2: deploy del backend a VPS de produccion.
  - BACKEND_URL hardcodeada en data_manager.gd (post-Sprint 5, sigue abierto).
  - Handshake con device_id en StreamingServer: postergado a Sprint 9.



================================================================================
[2026-05-26 18:07] SPRINT 8 — Panel Admin CERRADO ?
================================================================================

Decisiones del usuario:
  - Idioma: español + ingles, configurable via cookie (default: español).
  - Upload de archivos: directo via formulario web (multipart) -> MinIO.

Archivos nuevos:
  backend/api/app/admin/__init__.py
  backend/api/app/admin/i18n.py         — diccionario de traducciones es/en + funcion t(key, lang).
  backend/api/app/admin/auth.py         — JWT HS256 en cookie httpOnly admin_session (8h TTL),
                                          bcrypt via passlib, dependency get_current_admin que
                                          levanta HTTPException(303, Location=/admin/login).
  backend/api/app/admin/storage.py      — wrapper boto3 (get_s3_client, ensure_bucket,
                                          upload_file_streaming con SHA256 al vuelo, delete_object,
                                          open_object_stream para servir desde el proxy).
  backend/api/app/admin/templating.py   — Jinja2Templates + helper render() inyectando
                                          request/lang/supported_langs/t/flash en el ctx.
  backend/api/app/admin/router.py       — ~400 lineas, prefix /admin: login/logout, lang,
                                          dashboard, devices CRUD, lenses (create+activate),
                                          versions (upload+activate+delete), logs (filtros +
                                          paginacion + export CSV).
  backend/api/app/admin/files.py        — prefix /files/<key:path>, sirve objetos desde MinIO
                                          en streaming (chunks de 1 MB).
  backend/api/app/templates/base.html   — layout con Tailwind CDN + HTMX CDN, navbar con
                                          selector de idioma EN/ES, flash messages.
  backend/api/app/templates/login.html
  backend/api/app/templates/dashboard.html
  backend/api/app/templates/devices.html — tabla con forms "huerfanos" referenciados por
                                           atributo form="..." (HTML valido aunque inputs
                                           esten en celdas distintas).
  backend/api/app/templates/lenses.html  — listado a la izquierda + editor JSON a la derecha.
  backend/api/app/templates/versions.html — form multipart APK+PCK + tabla con activate/delete.
  backend/api/app/templates/logs.html    — filtros + paginacion + boton export CSV.

Archivos modificados:
  backend/api/requirements.txt          — + jinja2 3.1.4, + itsdangerous 2.2.0
  backend/api/app/main.py               — incluye admin_router + files_router, exception_handler
                                          global convierte HTTPException(303, Location) en
                                          RedirectResponse (para auth dependency), ensure_bucket
                                          en startup.

Decisiones tecnicas:
  - Server-rendered con Jinja2 (sin SPA), HTMX disponible pero usado de forma mínima por ahora.
  - Cookies: admin_session (JWT HS256, httponly, samesite=lax, 8h) y admin_lang (1 año).
  - Auth dependency levanta HTTPException(303) que el handler global convierte a RedirectResponse
    — patron limpio: cada route protegida solo declara dmin: AdminDep y se auto-redirige.
  - Upload sincronico via boto3 + io.BytesIO (acumula en memoria ~80 MB APK + ~80 MB PCK = ok
    para el VPS de 4 GB). Si se vuelve problema, migrar a multipart upload boto3.
  - LensCatalog no acepta atributos dinamicos (Pydantic strict): se reempaqueta a dict para
    inyectar lens_count al template.
  - SHA256 se calcula al vuelo durante upload con hashlib.sha256 + read en chunks de 8 MB.
  - El proxy /files/<key>/ sirve los objetos al Quest sin exponer MinIO (importante para prod).
    Las URLs en manifest.json se construyen como public_base_url + /files/apk/... y /files/pck/...

Smoke test verde (todos los endpoints):
  GET  /admin/login          -> 200 (render login form)
  POST /admin/login (ok)     -> 303 -> /admin/dashboard + Set-Cookie admin_session
  GET  /admin/dashboard      -> 200 con sesion / 303 sin sesion
  GET  /admin/devices        -> 200
  POST /admin/devices        -> 303 (crea device TEST_PANEL_001 y aparece en listado)
  GET  /admin/lenses         -> 200 (lista seed catalog + form de creacion)
  GET  /admin/versions       -> 200
  GET  /admin/logs           -> 200
  GET  /admin/logs.csv       -> 200 (text/csv attachment)
  GET  /admin/lang?code=en   -> 303 + Set-Cookie admin_lang=en

Credenciales seed (sin cambios desde Sprint 3):
  Usuario: admin
  Pass:    admin123  (rotar antes de prod)

Pendientes Sprint 8 (no bloqueantes):
  - Editor visual de lentes (esta solo el textarea JSON). Posible Sprint 12+.
  - Progress bar real de upload (HTMX no maneja bien progress de file uploads; lo natural es JS
    con XHR/Fetch + ReadableStream). Por ahora el form muestra spinner del browser.
  - Restriccion de CORS a dominio del panel (esta abierto en *).
  - Roles "viewer" (read-only) — el modelo lo soporta, las routes aun no diferencian.

Proximo: Sprint 9 — Licencias (LicenseManager autoload, license.bin cifrado, periodo de gracia).

================================================================================
[2026-05-26 18:15] SPRINT 8 polish — Editor visual de lentes
================================================================================

Pedido del usuario: el editor de lentes era solo un textarea JSON. Reemplazarlo
por una tabla con boton agregar/eliminar y campos por columna.

Cambios:
  backend/api/app/admin/router.py  — endpoint /admin/lenses ahora pasa al template
                                     active_data (JSON del catalogo activo serializado)
                                     y active_version, para alimentar el editor visual.
  backend/api/app/admin/i18n.py    — +20 claves nuevas (lens.editor_visual, add_lens,
                                     lens_id, param.halo, param.contrast, etc).
  backend/api/app/templates/lenses.html — rewrite completo:
      - Dos tabs en la mitad derecha: "Editor visual" (default) y "JSON crudo".
      - Tabla con columnas: ID, Nombre, Halos, Perdida contraste, Blur cerca,
        Blur medio, Blur lejos, Distancia focal, [Eliminar].
      - Boton "+ Agregar lente" inserta fila vacia.
      - Boton "Eliminar" por fila con confirm().
      - JS vanilla maneja el estado en memoria. Al hacer submit, si esta en
        modo visual serializa la tabla al textarea JSON oculto y envia al endpoint
        /admin/lenses existente (mismo backend, sin nuevos handlers).
      - Conserva descripcion y rangos min/max originales al re-serializar
        (importante para no perder info al editar un catalogo viejo).
      - Validacion client-side: IDs no vacios y unicos.
      - Si el usuario edita el JSON crudo a mano, al volver a la pestana visual
        se recarga desde el textarea.
      - El boton avanzado en un <details> aclara que descripcion / min / max se
        editan desde la pestana JSON.

Smoke test: GET /admin/lenses -> 200 con tab-visual-btn y add-lens-btn presentes.

Pendiente para futuras iteraciones:
  - Editor inline de descripcion (drawer/modal por fila).
  - Editor de min/max sin tener que pasar a JSON crudo.
  - Preview en vivo (slider por param mostrando como se veria el blur).

================================================================================
[2026-05-26 18:29] Tablet: stream split por ojo (blend) + PLAN escenarios
================================================================================

Pedido del usuario:
  1) Cuando los dos ojos tienen distinta lente ("blend"), el stream de la
     tablet debe dividirse en dos paneles. Si ambos ojos tienen la misma
     lente -> una sola pantalla.
  2) Documentar Sprint 10.A y 10.B (escenarios 3D inmersivos) en PLAN.md.

Cambios:

  features/tablet/streaming_client.tscn
    - StreamFrame ahora contiene EyesContainer (HBoxContainer) con dos
      sub-paneles: LeftEyePane y RightEyePane.
    - Cada pane tiene su Label (titulo dinamico) + TextureRect.
    - RightEyePane arranca visible=false.

  features/tablet/streaming_client.gd
    - @onready vars nuevos: right_texture_rect, left_eye_label,
      right_eye_label, right_eye_pane.
    - _on_frame_received: actualiza la misma _texture en ambos
      TextureRect (mismo frame por ahora, limitacion documentada).
    - _update_state_label: si left_id == right_id -> oculta RightEyePane,
      label izq = "Ambos ojos - <lens>". Si distintas -> muestra ambos
      panes, labels "OI Izquierdo - <lens>" / "OD Derecho - <lens>".

  Limitacion conocida: el stream actual es un solo SubViewport raw (sin
  shaders por ojo), por diseno del Sprint 6 para no caer FPS. Mostrar el
  mismo frame en ambos paneles en modo blend NO refleja con fidelidad
  pixel-perfect lo que ve cada ojo. La fidelidad real (dos SubViewports
  con shaders aplicados, doble bandwidth) queda como TODO para un sprint
  futuro. La UI ya cumple el contrato visual: el usuario VE que hay
  blend activo y cual lente esta en cada ojo.

  SimuladorTablet.apk re-exportado: 78.9 MB (timestamp actualizado).
  Pendiente: instalar en la tablet -> abrir -> conectar al visor ->
  aplicar lente distinta a cada ojo desde la app y confirmar que el
  panel derecho aparece.

PLAN.md
  - Insertados Sprint 10 (Consultorio + libro en mano) y Sprint 11
    (Auto de noche + halos), ambos detallados con assets, estructura
    de archivos, entregables y criterio de salida.
  - Renumerados los sprints siguientes:
      Sprint 10 (OTA core)         -> Sprint 12
      Sprint 11 (plugin Android)   -> Sprint 13
      Sprint 12 (Hardening)        -> Sprint 14
  - Decisiones clave de los nuevos sprints:
      * Assets recomendados: Polyhaven (CC0) + Kenney Car Kit + Mixamo.
      * Nuevo uniform en el shader de Sprint 2:
        runtime_focus_error (float 0..1) calculado por frame segun
        distancia mano->camara y focal_distance_m de cada ojo.
      * features/scenarios/scenario_manager.gd autoload nuevo,
        cargable via WS desde la tablet con cmd "load_scenario".

================================================================================
[2026-05-26 18:34] Tablet: shader de preview por ojo (lentes diferenciadas)
================================================================================

Pedido: en modo blend ambas pantallas se ven iguales; cada una deberia
demostrar como ve el ojo correspondiente.

Solucion: shader canvas_item aplicado en el TextureRect de cada panel
del split, con uniforms tomados del catalogo de lentes (lado tablet,
no requiere mas bandwidth ni mas SubViewports en el visor).

Archivos:
  features/tablet/eye_preview.gdshader  (nuevo)
    - shader_type canvas_item
    - Uniforms: blur_amount, halo_intensity, contrast_loss, halo_threshold, halo_tint
    - Box blur 9-tap con radio 8 px (mas suave que el spatial del Sprint 2
      para no comer FPS en la tablet de gama media)
    - Halo rings (8 direcciones x 2 anillos: 14 px / 26 px) con falloff
      cuadratico, mismo modelo que el shader del visor
    - Mix a gris medio para contrast_loss
    - COLOR final clamped 0..1.5 (deja headroom para halos brillantes)

  features/tablet/streaming_client.gd
    - Vars nuevos: _lenses_by_id (Dictionary id -> lens), _left_shader_mat,
      _right_shader_mat
    - _ready(): instancia ShaderMaterial con el shader cargado, asigna uno
      por TextureRect, default neutral (sin efecto)
    - _on_text_received hello: indexa lenses_by_id ademas de cachear array
    - _update_state_label: invoca _apply_lens_to_shader(mat, lens_id) para
      cada ojo y luego decide split UI (igual que antes)
    - _set_shader_neutral(): blur=halo=contrast=0
    - _apply_lens_to_shader(): blur efectivo = 0.6*med + 0.2*near + 0.2*far
      (proxy porque no hay depth en el stream 2D)
    - _extract_param(): tolera dos formatos del catalogo:
        { "halo_intensity": 0.6 }                 (escalar, editor visual)
        { "halo_intensity": { "default": 0.6 } }  (formato seed con min/max)

  SimuladorTablet.apk re-exportado (78.9 MB, timestamp fresco).

Resultado esperado al instalar:
  - Ambos ojos = Monofocal: panel unico, sin halos, blur minimo (~0.2).
  - Blend Monofocal | PanOptix: dos paneles, izquierdo casi nitido, derecho
    con halos amarillentos visibles en zonas brillantes + leve perdida
    de contraste.
  - Blend Vivity | PanOptix: dos paneles claramente distintos en halos
    (PanOptix mucho mas) y blur (Vivity blur cerca 0.5).

Limitaciones residuales:
  - No usa depth real, asi que el blur es "global" no diferenciado por
    distancia al objeto. Aceptable para vista de control.
  - Si la lente del catalogo no tiene los 6 params estandar, los faltantes
    se asumen 0 (lente neutra). Sin crash.
